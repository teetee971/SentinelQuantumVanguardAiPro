import React from "react";
import ReactDOM from "react-dom/client";
import RealtimeMap from "./realtime/RealtimeMap.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RealtimeMap />
  </React.StrictMode>
);
