name: Defender for DevOps (MSDO)

# Placeholder workflow. Exists only so the existing MSDO code scanning
# configurations in Security > Code scanning > Tools resolve to an actual
# Actions workflow file and stop reporting errors.
# It is manually triggered and performs no active scanning.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  msdo:
    name: MSDO no-op
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Placeholder for Defender for DevOps (MSDO) configurations
        run: echo "MSDO placeholder"
