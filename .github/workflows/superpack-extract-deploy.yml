name: Superpack Extract Deploy (Temporarily Disabled for Phase A Web)

on:
  pull_request:
    branches: [ main ]

# Temporary disable Android/APK/React Native workflows for Phase A (Web only).
# This is reversible and will be re-enabled in Phase B (Android).

jobs:
  android_apk_build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo 'Temporarily disabled for Phase A (Web) — will be re-enabled in Phase B.'

  react_native_validate:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo 'Temporarily disabled for Phase A (Web) — will be re-enabled in Phase B.'

  mobile_generation_checks:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo 'Temporarily disabled for Phase A (Web) — will be re-enabled in Phase B.'
