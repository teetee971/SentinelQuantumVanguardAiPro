name: CI Smoke

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  smoke:
    name: CI smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Show runner info
        run: |
          echo "GITHUB_REPOSITORY=${GITHUB_REPOSITORY}"
          echo "RUNNER_NAME=${RUNNER_NAME}"
          echo "RUNNER_OS=${RUNNER_OS}"
          echo "RUNNER_ARCH=${RUNNER_ARCH}"
      - name: Node and npm versions
        run: |
          node --version
          npm --version
      - name: Filesystem quick check
        run: |
          test -f package.json
          test -f index.html
          echo "CI_SMOKE_OK"
