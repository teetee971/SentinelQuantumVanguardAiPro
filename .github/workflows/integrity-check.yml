name: Disabled (Phase B isolation)

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled during Phase B isolation"