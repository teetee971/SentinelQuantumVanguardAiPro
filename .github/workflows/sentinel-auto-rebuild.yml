name: 🚀 Sentinel Autonomous Rebuilder + Cleaner + Telegram + Health Metrics

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"  # Exécution automatique toutes les 6h

jobs:
  sentinel_rebuild:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 🧠 Checkout du dépôt
        uses: actions/checkout@v4

      - name: 🧹 Nettoyage des sous-modules fantômes
        run: |
          echo "🔍 Nettoyage des sous-modules obsolètes..."
          git submodule deinit -f --all || true
          rm -rf .git/modules/akiprisaye || true
          git rm -f akiprisaye || true
          rm -rf akiprisaye || true
          echo "✅ Sous-modules nettoyés avec succès."

      - name: 🕒 Démarrage du chronomètre
        id: start
        run: echo "time_start=$(date +%s)" >> $GITHUB_ENV

      - name: 🛠️ Installation des dépendances
        run: |
          echo "📦 Installation des dépendances..."
          npm install

      - name: 🧪 Build du projet
        run: |
          echo "🏗️ Compilation du projet Sentinel..."
          npm run build || echo "⚠️ Avertissement : build partiel"

      - name: 🕒 Fin du chronomètre
        id: end
        run: |
          echo "time_end=$(date +%s)" >> $GITHUB_ENV
          duration=$(( $time_end - $time_start ))
          echo "duration_seconds=$duration" >> $GITHUB_ENV

      - name: 🌐 Déploiement Cloudflare Pages
        run: |
          echo "🚀 Déploiement automatique sur Cloudflare Pages..."
          npx wrangler pages deploy dist \
            --project-name=sentinelquantumvanguardaipro \
            --branch=main || true
          echo "✅ Déploiement terminé."

      - name: 📤 Commit de synchronisation
        run: |
          git config --global user.name "SentinelAutobot"
          git config --global user.email "sentinel@system.local"
          git add -A
          git commit -m "🧹 Auto-clean + Rebuild Sentinel Quantum Vanguard AI Pro" || true
          git push origin main || true

      - name: 🧮 Collecte des métriques système
        id: metrics
        run: |
          CPU=$(top -bn1 | grep "Cpu(s)" | sed "s/.*, *\([0-9.]*\)%* id.*/\1/" | awk '{print 100 - $1"%"}')
          RAM=$(free -m | awk '/Mem:/ {printf "%.1f Go (%.1f%%)", $3/1024, $3/$2*100}')
          DURATION=$(($time_end - $time_start))
          echo "CPU=$CPU" >> $GITHUB_ENV
          echo "RAM=$RAM" >> $GITHUB_ENV
          echo "DURATION=$DURATION" >> $GITHUB_ENV
          echo "CPU Utilisation : $CPU"
          echo "RAM Utilisation : $RAM"
          echo "Durée du build : $DURATION secondes"

      - name: 📡 Envoi à SentinelVanguardAIBot
        if: always()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          STATUS="${{ job.status }}"
          if [ "$STATUS" = "success" ]; then
            ICON="🟢"
            COLOR="Succès total"
          else
            ICON="🔴"
            COLOR="Erreur détectée"
          fi

          CPU="${CPU:-N/A}"
          RAM="${RAM:-N/A}"
          DURATION="${DURATION:-N/A}"

          MESSAGE="🚀 *Sentinel Auto-Rebuilder terminé*\n\n${ICON} *Statut:* ${COLOR}\n🧠 *Branche:* main\n⏱️ *Durée:* ${DURATION}s\n⚙️ *CPU:* ${CPU}\n💾 *RAM:* ${RAM}\n🌐 [Voir le déploiement](https://sentinelquantumvanguardaipro.pages.dev)\n\n📊 _Render Health transmis à Sentinel Telemetry Core_"
          curl -s -X POST https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage \
            -d chat_id=${TELEGRAM_CHAT_ID} \
            -d parse_mode="Markdown" \
            -d text="${MESSAGE}"

      - name: 🧾 Upload du rapport
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-build-report
          path: |
            dist
            package.json
          retention-days: 7