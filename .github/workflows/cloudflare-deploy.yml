name: 🚀 Deploy Sentinel Quantum Vanguard AI Pro (Cloudflare Auto)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout du dépôt
        uses: actions/checkout@v4

      - name: 🧩 Installation des dépendances
        run: npm ci

      - name: 🏗️ Build du projet Vite (React + Tailwind)
        run: npm run build

      - name: 🧹 Création du rapport d’audit
        run: |
          mkdir -p dist/audit
          echo "✅ Build success at $(date)" > dist/audit/status.txt

      - name: ☁️ Déploiement vers Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: sentinelquantumvanguardaipro
          directory: dist
          branch: main

      - name: 🧼 Purge du cache Cloudflare
        run: |
          curl -X POST "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
          -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
          -H "Content-Type: application/json" \
          --data '{"purge_everything":true}'

      - name: 📢 Notification Telegram
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          SITE_URL="https://sentinelquantumvanguardaipro.pages.dev"
          MESSAGE="✅ *Déploiement réussi !*\n🌐 ${SITE_URL}\n🕒 $(date '+%H:%M:%S (heure Guadeloupe)')"
          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d parse_mode="Markdown" \
            -d text="$MESSAGE"